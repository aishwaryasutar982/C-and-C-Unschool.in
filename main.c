#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c,sum;
    printf("Enter a Three no.:");
    scanf("%d %d %d",&a,&b,&c);

    sum=a+b+c;
    printf("Sum = %d",sum);
    return 0;
}
